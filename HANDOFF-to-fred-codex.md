# HANDOFF -> Fred-Codex: finish the spike_chat Hermes platform plugin (last mile)
**From:** Omar-Claude · **Date:** 2026-06-24 · Built ~90%, stuck on gateway-enablement (your domain)

## What it is + where
- Repo (git-first): **github.com/Willabor/hermes-spike-chat-plugin** (public, no secrets).
- Installed on Mac: `~/.hermes/plugins/spike-chat-platform/` via `hermes plugins install Willabor/hermes-spike-chat-plugin`; `hermes plugins enable spike-chat-platform` done.
- Goal: Queen's WARM persistent gateway joins spike-chat directly (replaces the flaky cold `-z` relay). Adapter polls `GET {url}/api/feed?member=Queen`, builds a MessageEvent -> `handle_message` (warm session), replies via `POST {url}/api/messages`.

## Architecture (done + verified)
- `adapter.py`: `class SpikeChatAdapter(BasePlatformAdapter)` — connect / _listen_loop / disconnect / send / get_chat_info + build_source + MessageEvent. Uses `Platform.RELAY` (generic) since `spike_chat` is not a built-in `Platform` enum value.
- `register(ctx)` calls `ctx.register_platform(name="spike_chat", ..., env_enablement_fn=_env_enablement, ...)`.
- `_env_enablement()` reads `SPIKE_CHAT_URL / _MEMBER / _PROJECT / _CHANNEL / _ROLE / _POLL_SECONDS` env -> returns the seed for `PlatformConfig.extra` (+ a `home_channel`). **VERIFIED in isolation:** `SPIKE_CHAT_URL=x SPIKE_CHAT_MEMBER=Queen venv/bin/python -c "import adapter; print(adapter._env_enablement())"` returns the dict. (Fixed a real bug: missing `import os`.)

## THE BLOCKER (last mile — your Hermes-internals expertise)
Gateway logs **"No messaging platforms enabled"** — spike_chat never connects, and there is NO spike registration/discovery line in `~/.hermes/profiles/queen/logs/gateway.log`. The env hook works standalone, but the gateway is not enabling the platform.

**What I tried (none worked):** `SPIKE_CHAT_*` in the profile `.env`, the global `~/.hermes/.env`, AND the queen gateway launchd plist `EnvironmentVariables` (via PlistBuddy). `ps eww <gateway-pid>` shows 0 SPIKE_CHAT (dotenv may load into os.environ at runtime, invisible to ps).

**Key questions for you:**
1. Does the gateway actually LOAD enabled platform plugins and call their `register()` + `env_enablement_fn`? (No spike discovery line in the gateway log suggests maybe not — or the registry the gateway consults differs from the one `hermes plugins` writes.)
2. When does the gateway load env into `os.environ` relative to the platform-enablement loop? (If env loads AFTER the loop, `_env_enablement` sees nothing.)
3. Per `gateway/platforms/ADDING_A_PLATFORM.md`, maybe `apply_yaml_config_fn` (own the config.yaml schema) is the right/additional hook. The bundled `irc` plugin uses `env_enablement_fn`; check `teams` / `google_chat` for the yaml-config path.

## Current state / guardrails
- STOPGAP restored + LIVE: `com.nexus.queen-spike-relay` + `com.nexus.queen-keepwarm` (launchd) — Queen answers in #order-ops via cold `-z` (flaky but working). Retire them ONLY once the plugin connects.
- Queen gateway plist has SPIKE_CHAT env injected (harmless if unused; regenerated by `gateway install`).
- Iterate git-first: edit repo -> push -> `hermes plugins update spike-chat-platform` -> `hermes --profile queen gateway restart`.
- Telemetry footer (time · tools · count) is a planned follow-up (the base runs the agent; the footer hook belongs in the message pipeline / a response wrapper).
